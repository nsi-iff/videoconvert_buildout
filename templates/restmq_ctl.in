#!/bin/bash

PID=${restmq:pidfile}
start(){
  ${buildout:directory}/restmq/restmq_server
}

stop(){
  kill -15 `cat $PID`
}

case "$1" in
  start)
    start;;
  stop)
    stop;;
  restart)
    start
    stop;;
  *)
  echo "Usage: restmq_ctl {start|stop|restart}"
  exit 1;;
esac
exit

